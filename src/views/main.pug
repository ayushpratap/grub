//  TODO : show navbar
//  TODO : list all the registered users

<!DOCTYPE html>
html(lang="en")
    //  Include the head
    include head.pug
    //  Add socket.io client
    script(src="/socket.io/socket.io.js")
    script(src="/js/main-script.js")

    body
        .container-fluid
            .container
                //  Create first row
                .row
                    .col.col-sm-9.col-lg-9.col-md-9
                        h3 Username : 
                            span(id='username') #{username}
                    .col.text-center
                        a(href="/logout") Logout
                hr
                //  Create second row
                .row
                    .col.col-sm-3.col-lg-3.col-md-3.border
                        //  Create user list dynamically 
                        div(id='user-list' class='container')
                            //each user in userList
                                if user.username != username
                                    .row
                                        a(href="#")
                                            = user.username
                    .col.col-sm-9.col-lg-9.col-md-9
                        .container
                            //  Create a row
                            .row
                                //  Add one column
                                .col-sm-12.col-md-12.col-lg-12
                                    //  Add card
                                    .card
                                        //  Add card header
                                        .card-header.text-center
                                            p   Test User
                                        //  This is the card body
                                        .card-block.chat-box-messages
                                            p   This is the messaging body
                                            p   This is the messaging body
                                            p   This is the messaging body
                                            
                                        //  Add card footer
                                        .card-footer
                                            //  Add row
                                            .row.text-center
                                                // Add input box
                                                .form
                                                    .form-group.col-sm-6.col-md-6.col-lg-6.text-center
                                                        input(type="text", name="test") 
                                                    .form-group.col-sm-6.col-md-6.col-lg-6.text-center
                                                        button(type="submit") Send